name: PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  Build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v3

      - uses: akiojin/auto-review-github-action@v0.2.1
        with:
          openai-api-key: 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
          target: "cs,yml"
          language: 日本語
